import { fileUpload } from "../../src/helpers/fileUpload";

describe('Pruebas en la funcion uploadFile', () => {

    // TODO:REVISAR CARGA DE IMAGEN
    test('Debe subir la imagen correctamente', async () => {


        const imageUrl = "https://ticnegocios.camaravalencia.com/wp-content/uploads/2017/09/que-es-crm.jpg"
        // const res = await fetch(imageUrl);

        // const blob = await res.blob();

        // const imagen = new File([blob], "foto.jpg");
        // await fileUpload(imagen);

    })
});